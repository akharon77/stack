Ошибки Даниила.

Как пытался поломать:

1. Сделал 100 push, 101 pop
   
   Причина: не проверялся стэк на корректность, то есть больше ли size нуля

   Местоположение - функция Stack_pop_, в самом начале.

   Ошибка: запланированная, легкая

2. Чередовал push, pop, 
   то есть 10 push, 5 pop, 2 push, 1 pop
   
   Обработалось корректно

3. Поменял поле size на -1

   Обработалось корректно, так как проверилось верификатором во всех функциях, кроме pop, о чем сказано выше
   
   Замечание: ошибка не вывелась в логи

4. Поменял capacity на -1

   Ошибка не обработалась, стек упал с SEGFAULT

   Причина: перевод -1 в беззнаковое число, что приводило к переполнению и сбою

   Местоположение: Check_hash_data

   Ошибка: запланированная? сложная

5. Насильно поменял stk.data[1] = 1

   Обработалось корректно

6. Вызвал StackCtor с отрицательным значением capacity
   
   Упало с ошибкой

   Причина: не обрабатывалось, может ли быть capacity меньше нуля

   Местоположение: StackCtor

   Ошибка: незапланированная
